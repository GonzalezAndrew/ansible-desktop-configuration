# Dotfile

```
$ git clone https://github.com/GonzalezAndrew/dotfiles && cd dotfiles
# install xcode-select, brew, etc 
$ ./brew-install.sh 

# Remove README && brew install script
$ rm READM && rm brew-install.sh

# link dotfiles 
$ stow -vt ~ *

# run to enable power10k
$ 'builtin' 'source' "${POWERLEVEL9K_INSTALLATION_DIR:-${${(%):-%x}:A:h}}/powerlevel10k.zsh-theme"


```
